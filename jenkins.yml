version: '2.11'

services:
  zero:
    build: .
    expose:
      - "5000"
    environment:
      - "ENV=PROD"
    links:
      - "dnmonster"
      - "redis"
  
  dnmonster:
    image: amouat/dnmonster:1.0

  redis:
    image: redis:3.0
